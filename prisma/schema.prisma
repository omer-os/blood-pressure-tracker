generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model BloodPressure {
  id        String   @id @default(cuid())
  systolic  Int
  diastolic Int
  pulse     Int?
  createdAt DateTime @default(now())
}
